<!doctype html>
<html>
  <head>
    <title>Dental Database</title>
  </head>
  <body>

    <div class="container">
      <% if flash[:errors] %>
      <h3 style="color: red;"><%= flash[:errors] %></h3>
      <% end %>
      <%= yield %>
    </div>

  </body>
</html>
